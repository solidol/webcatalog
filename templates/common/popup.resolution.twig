<div id="orgModal" class="modal fade">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header"><button class="close" type="button" data-dismiss="modal">×</button>
                <h4 class="modal-title">Додати резолюцію</h4>
            </div>
            <form action="" method="post" name="addoresform" id="addoresform">

                <div class="modal-body">
                    <div class="container-fluid">
                        <fieldset>
                            <input type="hidden" name="doc" value="{{doc.id}}" />
                            <div class="row">
                                <!-- Select Basic -->
                                <div class="form-group">
                                    <label class="col-md-4 control-label" for="user">Оберіть виконавця</label>
                                    <div class="col-md-6">
                                        <select id="user" name="user" class="form-control" required="required">
                                            <option value="-1" disabled="disabled" selected="selected">Оберіть виконавця</option>
                                            {% for item in userlist %}
                                                <option value="{{item.id}}">{{item.name}}</option>
                                            {% endfor %}
                                        </select>
                                    </div>
                                </div>

                            </div>

                            <div class="row">
                                <div class="form-group">
                                    <label class="col-md-4 control-label" for="text">Текст резолюції</label>
                                    <div class="col-md-6">                     
                                        <textarea class="form-control" id="textres" name="textres"></textarea>
                                    </div>
                                </div>                     
                            </div>


                        </fieldset>
                    </div>
                </div>
                <div class="modal-footer">
                    <button class="btn btn-success" id="btnpostresolution" type="button" data-dismiss="modal">Зберегти</button>
                    <button class="btn btn-default" type="button" data-dismiss="modal">Скасувати</button>
                    <script>
                        $(document).ready(function () {
                            $("#btnpostresolution").click(function () {
                                $("#addoresform").ajaxSubmit({url: '{{path('ajax.resolution.push')}}', type: 'post'});
                                setTimeout(function(){location.reload();}, 500);
                            });

                        });
                    </script>
                </div>
            </form>
        </div>
    </div>
</div>
